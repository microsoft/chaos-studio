{
  "name": "@azure/chaos-studio-common",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript orchestration core for the Azure Chaos Studio v2 CI/CD integrations. Owns all service behavior behind injected adapter interfaces; imports no platform SDK (@actions/core or azure-pipelines-task-lib).",
  "license": "MIT",
  "type": "module",
  "main": "src/contract.ts",
  "exports": {
    ".": "./src/contract.ts"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json"
  }
}
