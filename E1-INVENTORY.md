# E1 change inventory — task traceability (E1-T1 … E1-T5)

This is the **authoritative inventory** for epic **E1 — Existing-repo integration +
source-contract fixtures**. Every file the epic adds or modifies appears in **exactly
one** task table below, under the task that owns the file. Files that additionally
contribute to another task are cross-referenced in the separate
"[Cross-task coverage](#cross-task-coverage-no-additional-files)" section — never as a
second inventory row. A reviewer can diff the per-task tables against `git status` and
find each changed path exactly once, with no untraced change and no missing entry.

Legend: **new** = added by E1; **mod** = pre-existing file modified by E1.

## E1-T1 — npm-workspaces skeleton, root Action stub, TS paths, committed bundles, extension manifests, permanent task GUID

| File | State | Purpose |
|---|---|---|
| `package.json` | new | Root npm-workspaces manifest (`workspaces: ["packages/*"]`, Node ≥24), coexisting with the Python/PowerShell tooling. |
| `package-lock.json` | new | Locked workspace dependency graph for reproducible `npm ci`. |
| `tsconfig.base.json` | new | Shared strict TypeScript compiler options. |
| `tsconfig.json` | new | Root solution config that references the workspace packages. |
| `packages/core/package.json` | new | Shared core package manifest. |
| `packages/core/tsconfig.json` | new | Core package TypeScript config. |
| `scripts/build.mjs` | new | Single deterministic producer of the committed `dist/**` bundles (keeps the release dist-integrity gate effective). |
| `dist/github-action/index.js` | new | Committed GitHub Action runtime bundle path (fail-fast placeholder until E3). |
| `dist/github-action/README.md` | new | Explains the committed Action bundle path. |
| `dist/azure-pipelines-task/index.js` | new | Committed Azure Pipelines task runtime bundle path (fail-fast placeholder until E4). |
| `dist/azure-pipelines-task/README.md` | new | Explains the committed task bundle path. |
| `action.yml` | new | Root GitHub Action metadata stub, `runs.using: node24` (VF17), canonical inputs/outputs. |
| `azure-pipelines-extension/vss-extension.json` | new | Production Azure DevOps extension manifest; declares the mandatory public-Marketplace `content.details` (overview.md) and `icons.default` (128×128 PNG) and packages both as addressable files. |
| `azure-pipelines-extension/vss-extension.dev.json` | new | Development extension manifest (private, side-by-side); also declares content.details + icons.default and packages the shared assets. |
| `azure-pipelines-extension/overview.md` | new | Marketplace overview (`content.details`) describing the task's validate→execute→poll journey and requirements. |
| `azure-pipelines-extension/images/icon.png` | new | Marketplace extension icon (`icons.default`), a 128×128 PNG. |
| `azure-pipelines-extension/tasks/AzureChaosStudioScenarioV1/task.json` | new | Production task; **permanent GUID `83c40b4c-e5bb-4045-9cb2-ce389248b386`** generated once (VF15), `Node20_1` handler. |
| `azure-pipelines-extension/tasks/AzureChaosStudioScenarioV1Dev/task.json` | new | Development task; **distinct permanent GUID `eb1d4fc5-61c2-4887-a9af-a649907eaf64`** for side-by-side install. |
| `azure-pipelines-extension/README.md` | new | Extension packaging overview. |
| `.gitignore` | mod | Re-includes `/packages/**` past the NuGet `**/[Pp]ackages/*` rule; also re-includes the workspace SOURCE directory `packages/core/src/release/` past the VisualStudio `[Rr]elease/` build-results rule (which would otherwise ignore the source `release/` dir and silently drop `tagRulesetEval.ts`/`workflowPermsAudit.ts` from commits); preserves all current content paths. |
| `.gitattributes` | new | Pins the committed `dist/**` bundles (and, for E1-T3, the `packages/core/fixtures/source-extracts/**` extracts) to LF so the release dist-integrity gate and the extract provenance hashes are reproducible cross-platform. Single owning row; the E1-T3 contribution is noted under Cross-task coverage. |

## E1-T2 — canonical `contract.ts` types + adapter interfaces

| File | State | Purpose |
|---|---|---|
| `packages/core/src/contract.ts` | new | Types + source-proven constants only: `Inputs`/`INPUT_NAMES`, outputs, validation/run state machines, LRO constants, `PROVIDER_OPERATIONS`, and the adapter seams `IInputReader`/`IOutputSetter`/`ILogger`/`ICredentialProvider`/`IClock`. (Also carries the DX1–DX3 encodings — see E1-T4.) |

## E1-T3 — source-contract fixtures, reviewed source extracts, and tests

| File | State | Purpose |
|---|---|---|
| `packages/core/fixtures/validate/accept-202.json` | new | Validate acceptance (VF1). |
| `packages/core/fixtures/validate/accept-unevaluated-202.json` | new | Representative EXPECTED validate contract shape for an unevaluated workspace (202, removed 409 gate) — a hand-authored fixture, not a runtime capture (VF11). |
| `packages/core/fixtures/validate/validation-nonterminal-202.json` | new | Nonterminal `validations/latest` 202 (VF2, DX2). |
| `packages/core/fixtures/validate/validation-succeeded-200.json` | new | Terminal-success validation 200 (VF2, VF3). |
| `packages/core/fixtures/validate/validation-requires-attention-200.json` | new | Terminal-failure validation with dual error channels (VF3, VF10). |
| `packages/core/fixtures/validate/plan-mutation-sequence.json` | new | VF4 source-backed request/response sequence: an `If-Match` precondition is ignored and the plan is replaced. |
| `packages/core/fixtures/execute/accept-202.json` | new | Execute acceptance + run Location (VF5, DX3). |
| `packages/core/fixtures/execute/accept-unevaluated-202.json` | new | Representative EXPECTED execute contract shape for an unevaluated workspace (202, removed 409 gate) — a hand-authored fixture, not a runtime capture (VF11). |
| `packages/core/fixtures/execute/run-nonterminal-202.json` | new | Nonterminal run 202 with dual channels (VF6, DX2). |
| `packages/core/fixtures/execute/run-succeeded-200.json` | new | Terminal-success run 200 (VF6, VF7). |
| `packages/core/fixtures/execute/run-failed-200.json` | new | Terminal-failure run with dual channels (VF7, VF10). |
| `packages/core/fixtures/execute/run-transitions.json` | new | Run polling 202→200 transitions (VF6, VF7). |
| `packages/core/fixtures/cancel/accept-202.json` | new | Cancel acceptance to the same run resource (VF8, VF9). |
| `packages/core/fixtures/cancel/run-canceled-200.json` | new | Terminal `Canceled` run (VF7, VF8). |
| `packages/core/fixtures/cancel/run-transitions.json` | new | Cancel polling `Canceling`→`Canceled` (VF7, VF8). |
| `packages/core/fixtures/operations/provider-operations.json` | new | Full generated provider-operation surface (VF9, VF12, DX1). |
| `packages/core/fixtures/source-extracts/ScenarioValidationState.g.cs` | new | Reviewed extract: authenticates `VALIDATION_STATES` (VF3). |
| `packages/core/fixtures/source-extracts/ScenarioRunState.g.cs` | new | Reviewed extract: authenticates `RUN_STATES` (VF7). |
| `packages/core/fixtures/source-extracts/ValidationProperties.g.cs` | new | Reviewed representative extract: authenticates validation wire fields + `errors`/`validationErrors` channels (VF2, VF10, DX2). |
| `packages/core/fixtures/source-extracts/ScenarioRunProperties.g.cs` | new | Reviewed extract: run wire fields + channels (VF6, VF10, DX2). |
| `packages/core/fixtures/source-extracts/ConfigurationDomainLogicV1.cs` | new | Reviewed extract of `ValidateAsync`/`GetLatestValidationAsync`/`ExecuteAsync`: validate 202→`validations/latest`; validation GET 202/200; execute 202→`runs/{runId}` (VF1, VF2, VF4, VF5, DX3). |
| `packages/core/fixtures/source-extracts/RunDomainLogicV1.cs` | new | Reviewed extract of `GetRunAsync`/`CancelRunAsync`: run GET 202/200; cancel returns 202→the same run resource (VF6, VF8, VF9). |
| `packages/core/fixtures/source-extracts/ScenarioConfigurationsController.cs` | new | Reviewed BE controller extract: validate/execute return 202 and dispatch commands; no conditional-request or evaluation gate (VF4, VF11). |
| `packages/core/fixtures/source-extracts/StartScenarioValidationCommand.cs` | new | Reviewed BE handler extract: evaluation not required; validations/latest upserted with no precondition (VF4, VF11). |
| `packages/core/fixtures/source-extracts/StartScenarioExecutionCommand.cs` | new | Reviewed BE handler extract: evaluation not required; run id generated synchronously (VF5, VF11, DX3). |
| `packages/core/fixtures/source-extracts/CancelScenarioRunCommand.cs` | new | Reviewed BE cancel handler extract: state-aware cancel — repeated cancel and terminal runs no-op without throwing (idempotency + terminal no-op, VF8, VF9). |
| `packages/core/fixtures/source-extracts/ScenarioRunStateExtensions.cs` | new | Reviewed BE domain-predicate extract: `ScenarioRunState.IsTerminal()` is true for EXACTLY Succeeded/Failed/Canceled (and false for Canceling), authenticating the cancel handler's terminal no-op guard (VF7, VF8). |
| `packages/core/fixtures/source-extracts/ScenarioValidationStateExtensions.cs` | new | Reviewed BE domain-predicate extract: `ScenarioValidationState.IsTerminal()` is true for EXACTLY Succeeded/RequiresAttention/NoResolvedResources (and false for every advancing state), authenticating `GetLatestValidationAsync`'s 200-vs-202 branch and the `VALIDATION_TERMINAL_SUCCESS`+`VALIDATION_TERMINAL_FAILURE` sets (VF3). |
| `packages/core/fixtures/source-extracts/ScenarioValidationStore.cs` | new | Reviewed BE store extract: unconditional upsert of validations/latest, no version/precondition parameter (VF4). |
| `packages/core/fixtures/source-extracts/WorkspacesExceptionMapper.cs` | new | Reviewed BE exception-mapping extract: no PreconditionFailed/412 and no evaluation-required conflict for the validate/execute flow (VF4, VF11). |
| `packages/core/fixtures/source-extracts/ResourceSelectorResolver.cs` | new | Reviewed BE resolver extract: nullable evaluation-snapshot parameter, a null snapshot is accepted, no evaluation prerequisite (VF11). |
| `packages/core/fixtures/source-extracts/SelectorEvaluator.cs` | new | Reviewed BE live-evaluator extract: the null-snapshot delegate `ResolveLiveAsync`; the extracted body names no conflict/`409` token and no `throw` (static inspection, VF11). |
| `packages/core/fixtures/source-extracts/SelectorTargetQuery.cs` | new | Reviewed BE concrete terminal-callee extract: `QueryAsync` is a plain read whose extracted body names no conflict/`409` token and no `throw` (static inspection, VF11). |
| `packages/core/fixtures/source-extracts/ResolutionServiceRegistration.cs` | new | Reviewed BE DI-registration extract: binds each resolution interface to its concrete implementation so the authenticated callees are the ones the runtime resolves (VF11). |
| `packages/core/fixtures/source-extracts/SelectorTargetStore.cs` | new | Reviewed BE concrete-store extract: `ListTargetsAsync` is a read-only query (`ToListAsync`) whose extracted body names no write/optimistic-concurrency op, no conflict/`409`, and no `throw` (static inspection, VF11). |
| `packages/core/fixtures/source-extracts/SelectorMaps.cs` | new | Reviewed BE construction-helper extract: `ResolvedSelectorMap.From` / `SelectorMap.FromLiveTargets`; the extracted bodies name no conflict/`409`, no persistence/concurrency op, and no `throw` (static inspection, VF11). |
| `packages/core/fixtures/source-extracts/ScenarioErrors.g.cs` | new | Reviewed generated error models: exact nested error wire fields for the dual channels (VF10). |
| `packages/core/fixtures/source-extracts/ScenarioRunResource.g.cs` | new | Reviewed generated element model for the run `resources` object-array: closed field schema (`resourceId`/`targetType`/`selectors`) for recursive array-item validation (VF10). |
| `packages/core/fixtures/source-extracts/OperationIntegrationTestsV2026_05_01_preview.GetOperations.verified.txt` | new | Reviewed extract: authenticates the exact operation set (VF9, VF12, DX1). |
| `packages/core/fixtures/source-extracts/README.md` | new | How the extracts are the reviewed authority. |
| `packages/core/fixtures/provenance.manifest.json` | new | Fixture→source bindings, canonical fixture hashes, and per-source extract hashes. |
| `packages/core/fixtures/scripts/generate-provenance.mjs` | new | Regenerates the provenance manifest. |
| `packages/core/fixtures/PROVENANCE.md` | new | Provenance + extract-authentication model. |
| `packages/core/test/contract/contract.test.ts` | new | Asserts the contract constants (VF3, VF7, DX1, DX2, …). |
| `packages/core/test/contract/validate.test.ts` | new | Validate acceptance/resource + source-backed VF4 precondition behavior. |
| `packages/core/test/contract/run.test.ts` | new | Execute acceptance, run resource, DX3 run-ID parsing. |
| `packages/core/test/contract/cancel.test.ts` | new | Cancel acceptance + run transitions (VF7, VF8, VF9). |
| `packages/core/test/contract/operations.test.ts` | new | Exact operation-set authentication against the reviewed extract (VF9, VF12, DX1). |
| `packages/core/test/contract/provenance.test.ts` | new | Manifest integrity + extract authentication of contract & fixtures. |
| `packages/core/test/contract/manifest.test.ts` | new | Task/extension-manifest and root Action assertions (VF15, VF17). |
| `packages/core/test/contract/ado-pipeline.test.ts` | new | Parses the committed OneBranch.Official pipeline into an ORDERED TASK STRUCTURE (per stage, each task's id + inputs) with an indentation-aware parser — not textual slices — and locks the Real SignType request (`signing_profile: external_distribution`), BOTH stages' CodeSign Validation ENABLE flags (`ob_sdl_codeSignValidation_enabled`/`CodesignValidation: true`) + break-on gates + the environment-gated signing deployment, AND enforces VALIDATION-BEFORE-PUBLICATION ordering (the publish-stage CodeSign Validation + break-gate must precede the `PublishAzureDevOpsExtension@4` Marketplace task), so removing/reordering any gate fails CI; the parser computes `blockScalarContentLines` from the RAW lines and SKIPS YAML BLOCK-SCALAR content so a `- task:` inside a `run: |`/`script: |` script is not parsed as an executable task; it also parses JOBS (`- job:`/`- deployment:`/`- releaseJob:`) and BINDS each task to its executing job, so the sign/publish gates + ordering are asserted WITHIN THE SAME executing job (a gate in a sibling non-executing job does not satisfy the check); and it parses per-task `enabled:`/`condition:` (folded multiline task conditions gathered), per-stage `condition:`/`dependsOn:`, per-job `artifactName:` (`jobArtifacts`) and `environment:` (`jobEnvironments`), and the top-level `variables:` so the strict NON-NEGATED branch/publishExtension conditions (`assertStrictAndCondition` parses the root `and(...)` into its TOP-LEVEL conjuncts via `andConjuncts` and requires `succeeded()` + each branch/publishExtension predicate to be a DIRECT conjunct — a SEMANTIC check rejecting a DEAD/NESTED predicate or an `or(...)`-rooted/`always()`/`ne(`/`not(eq(` form), the sign→build/publish→sign dependencies, the CONCRETE `SigningEnvironment` bound to the signing task's OWN deployment job (no decoy), the enabled+unconditional gate tasks, the publish JOB's OWN signed-artifact consumption (`drop_sign_sign_vsix`, not the unsigned build drop), and the publish task's `vsixFile: $(SignedVsix)` input are all bound to ONE execution chain (pass 42 #2, pass 43 #6, pass 44 #9, pass 45 #7, pass 46 #3, pass 47 #3, pass 48 #3, pass 49 #3, pass 50 #1). The OneBranch condition checks are now EXACT (`assertStrictAndCondition(…, exact)` rejects a decoy always-true/always-false EXTRA conjunct), the SIGNING and PUBLISH critical tasks must be enabled + unconditional, `assertJobNotDisabled` (backed by a new per-job `jobConditions` parse) rejects a `condition: false`/decoy-gated signing/publish JOB, and a new test BINDS the published `$(SignedVsix)` to the single `*.vsix` found in `$(Pipeline.Workspace)/signed` (the signed-artifact download target AND the CodeSign Validation path, assigned exactly once) so a tampered/alternate copy cannot be published (pass 51 #3, #4). The parser now also models `continueOnError` per task and ALL steps (task/script/checkout, in source order with script bodies), and pass-52 tests require every CodeSign Validation/PostAnalysis gate to be BLOCKING (not `continueOnError: true`) and prohibit any executable script step or artifact-mutating step between the publish-job CodeSign Validation and the Marketplace publish (SignedVsix resolved before validation, never reassigned after) (pass 52 #3). The parser also models `- download:` shortcuts and download-task inputs (`downloadPath`/`path`), and the guard rejects ANY artifact-download step (`DownloadPipelineArtifact`/`DownloadBuildArtifacts`/`- download:`) — or a download whose `downloadPath`/`path` targets the signed dir — between validation and publication, so an unsigned artifact cannot replace the validated VSIX (pass 53 #3). |
| `packages/core/test/contract/release-fnmatch.test.ts` | new | Regression tests that extract the ACTUAL embedded ref-name FNM_PATHNAME matcher from both release workflows and pin its semantics — in particular that a TERMINAL `**` is NON-recursive, so a `refs/**` rule does NOT protect a nested tag ref (while `refs/tags/*`/`**`/`v*` still do) — and that both evaluators enforce NAMESPACE-WIDE coverage (a finite exact-tag list that passes the concrete-ref probes is rejected; a `refs/tags/<prefix>*` include with no excludes and the sole release-identity bypass covers it), gated by a committed `NAMESPACE_PREFIX` the tests assert cannot be silently removed (pass 42 finding #4). |
| `packages/core/test/contract/fixtures.ts` | new | Fixture loader helper. |
| `packages/core/test/contract/source-extracts.ts` | new | Lexically-aware parsers for the reviewed source extracts (strip comments + string/char literals before structural parsing). |
| `packages/core/test/contract/source-extracts.test.ts` | new | Adversarial unit tests proving the C# parsers are not fooled by comments, call sites, or braces inside strings/verbatim strings. Includes a ref-return dataflow false-positive guard, a static-readonly-412 scope test, a source-ordered composed-header test, a chained/fluent ref-return capture test, a static-readonly-412 partial-declaration + last-write-wins test, a point-of-use composed-header test, a single-exit dominance test, pass-53 (a REACHABLE static-ctor BRANCH 412 test, a PARENTHESIZED ref-capture/assignment provenance test, a `string.Join`/`string.Format`/`StringBuilder` composed-header test, a KNOWN-THROWING-guard dominance test), pass-54 corrective regressions (a BRACELESS-conditional static-ctor 412 branch test (R1), a VARIABLE StringBuilder FLUENT-continuation-chain + point-of-use test (R2), a `Guard.Against.Null(...)` MEMBER-CHAIN throwing-guard test with the nested-lambda exclusion preserved (R3)), a pass-55 StringBuilder-instance-REASSIGNMENT reset test, a pass-56 OBJECT/`with`-INITIALIZER-member-assignment test, pass-57 StringBuilder initializer-classification regressions (statement blocks — do/while, try/finally, local-function body — end tracking (F1); array/`List<>` collection-initializer assignment EXPRESSIONS reassign the local and end tracking (F2); a NESTED member initializer `new Foo { Child = { sb = 1 } }` preserves detection (F3)), and a pass-58 valid-object-initializer-forms test (ANONYMOUS `new { sb = 1 }`, TARGET-TYPED `new() { sb = 1 }` / `new(1,2) { sb = 1 }`, and ALIAS-QUALIFIED `new global::Ns.Foo { sb = 1 }` preserve detection while a statement-block reassignment still ends tracking), and a pass-59 escaped-`@new`-identifier test (a local function `void @new() { sb = … }` / a generic-return `Task<int> @new()` ends tracking — normalization drops the `@`, so `new` object-creation is distinguished from a `@new` method name by CONTEXT, an expression vs a return type — while a genuine `() => new() { sb = 1 }` still preserves detection). |
| `packages/core/test/contract/tagRulesetEval.test.ts` | new | Adversarial unit tests for the shared operation-aware LAYERED tag-ruleset evaluator: floating-tag `non_fast_forward` is required; layered rulesets are ANDed (a second applicable ruleset with a foreign bypass, or a restricting layer that does not grant the release identity a bypass, FAILS); ops may be spread across layers; inactive/non-tag rulesets and exclude carve-outs are honored; missing `bypass_actors` and unsupported fnmatch syntax fail closed; the bypass must be an `always` User with the exact actor id. Namespace evaluation additionally audits `patternOverlapsNamespace` and every OVERLAPPING (partial or whole) layer, so a restrictive layer covering only PART of the namespace (foreign bypass / missing identity) is not ignored while a disjoint layer is (pass 43 finding #4). |
| `packages/core/test/contract/workflowPermsAudit.test.ts` | new | Adversarial unit tests for the shared workflow-permissions auditor: a top-level or job-level `contents: write`/`packages: write`/`write-all` grant is a finding; `contents: read`/`id-token: write`/`read-all`/`{}` are clean; an allowlisted release workflow's narrow write grant is a NOTE only when its job is gated by a KNOWN-PROTECTED environment (an arbitrary/unknown environment name, an ungated workflow-level or job grant, is a finding even when allowlisted); MULTILINE FLOW mappings (`permissions: {` across lines), FOLDED-scalar `permissions:`/`uses:`, FOLDED/BLOCK-MAPPING and ANCHORED-FLOW-MAP anchor values, FLOW-FORM jobs/inline `permissions:` (failed-closed as un-gated), ESCAPED double-quoted keys, and POSITION-AWARE anchor resolution (an alias binds to the nearest PRECEDING definition, so a redefinition cannot hide a grant) plus ALIASED/unresolvable reusable-workflow `uses:` targets are all resolved/caught; a reusable-workflow `uses: …/*.yml` (including `./.github/workflows/x.yml`) is ALWAYS a finding while a step-level action `uses: owner/repo@sha` is not; a FLOW-FORM reusable workflow (`uses:` inside a flow map) and an ESCAPED `permissions` KEY inside a flow mapping are also caught; comments and dedented non-permissions keys are ignored. |

## E1-T4 — DX1–DX3 corrections

DX1–DX3 add **no new files**: the corrections are encoded in files already owned by
E1-T2 and E1-T3 (each listed once, above). This section records the coverage without
introducing duplicate inventory rows.

- **`packages/core/src/contract.ts`** (owned by E1-T2): DX1 `PROVIDER_OPERATIONS`
  (`configurations/execute/action`, and never `scenarios/run/action`); DX2
  `STATUS_FIELD` + `START_TIME_FIELD`/`END_TIME_FIELD` and the dual error-channel
  constants; DX3 `GUID_PATTERN` used to parse the run ID from the execute `Location`.
- **`packages/core/fixtures/**`** (owned by E1-T3): DX1 operations fixture + reviewed
  operations extract; DX2 `status`/`startTime`/`endTime` and dual-channel resource
  bodies; DX3 run `Location` GUIDs in the execute/cancel acceptance fixtures.

## E1-T5 — path-scoped CI, release namespaces, OneBranch pipelines, dependabot, ownership, docs, SHA pins

| File | State | Purpose |
|---|---|---|
| `.github/workflows/test.yml` | mod | Adds path-filtered TypeScript typecheck/test jobs alongside Pester/pytest/ruff; the filter includes the build-critical manifests, config, `scripts/**`, `dist/**`, `.config/**`, and `.pipelines/**`; the job runs the same empty-dir full-set + normalized-mode + git-blob-hash dist build-drift gate (rejecting non-regular rebuilt entries) as the release `validate` job; every third-party `uses:` is full-SHA pinned. |
| `.github/workflows/release.yml` | mod | Existing PyPI/MCP release is **`workflow_dispatch`-only** (a `version` input), gated by `if:` + an in-shell check to the default branch AND — the authoritative control — by EXTERNALLY configured protected environments (`pypi` for `publish-pypi`, `mcp-release` for `github-release`) with required reviewers + `prevent_self_review=true` + `can_admins_bypass=false` + default-branch-only deployment policies and environment-scoped credentials. The `github-release` job runs a fail-closed ENVIRONMENT-PROTECTION preflight (verifies BOTH `pypi` and `mcp-release` have those protections) that `publish-pypi` `needs:`, so an absent/unprotected environment fails the release before any PyPI upload or tag write. The `build` job verifies the input version equals the committed `pyproject.toml` version; on a RETRY where an `mcp-v*` tag already exists it resolves that tag's commit, requires it to be an ANCESTOR of the default-branch tip (trusted history), and BUILDS FROM that tagged commit (not the newer `github.sha`) — so a retry after the default branch advances recovers the original release instead of failing an equality check, and the `github-release` job re-resolves the tag and requires it to equal the built `release_commit`; the `github-release` job requires a CANONICAL `mcp-v<x.y.z>`, runs a fail-closed `mcp-v*` tag-ruleset preflight (token identity + FNM_PATHNAME coverage where a terminal `**` is NON-recursive + bypass visibility/foreign-actor checks, and — because LAYERED rulesets are ANDed by GitHub — a requirement that the release identity is a bypass actor in EVERY applicable RESTRICTING ruleset, not merely one via a union), creates the tag with BARE Git and verifies an existing tag's target (immutable, never moved), and — never trusting an existing published Release blindly — FIRST requires repository-level immutable releases to be ENABLED via a dedicated preflight step that runs BEFORE any tag write (so a disabled setting can never leave a mutable public tag/release) and CONFIRMS release immutability BEFORE re-verifying an already-published release's assets, then reconciles any unexpected leftover assets and verifies the exact asset SET, COUNT, and every sha256 DIGEST equal the locally built dist **while the release is still a DRAFT**, then publishes atomically and — after publication — requires GitHub to report the release IMMUTABLE (`isImmutable=true`) and RE-VERIFIES the exact asset set + digests on the public release before allowing (via `needs:`) the PyPI publish. The `publish-pypi` job is RETRY-SAFE after a partial upload: a reconcile step queries PyPI for the files already published for this version — parsing the JSON with `jq -e` into a captured variable (so a malformed/empty response FAILS CLOSED instead of looking like "nothing published" via a swallowed process-substitution error) and validating every entry's filename and 64-hex sha256 — FAILS CLOSED if a same-named file exists with a DIFFERENT sha256, drops digest-matching already-published files, and uploads ONLY the missing distributions. `skip-existing` is DELIBERATELY NOT set, so a race that uploaded a same-named file with different bytes 409s and fails rather than being silently skipped; an authoritative post-upload step then re-queries PyPI (bounded poll for API lag) and requires the COMPLETE published file set for the version to EQUAL the built manifest EXACTLY — every built distribution present with our exact sha256, failing CLOSED on a DUPLICATE filename, an UNEXPECTED/extra remote distribution, or a digest MISMATCH (only missing files are retried for propagation lag). The release token's own identity is verified fail-closed (no early-exit skip). The embedded FNM_PATHNAME matcher evaluates only GitHub's SUPPORTED ruleset subset and FAILS CLOSED on unsupported syntax (backslash escapes, `[^...]` class negation); within the subset it is Ruby-`File.fnmatch`-faithful (a `/` inside `[...]` is not a separator; a reversed range matches EXACTLY its two endpoints; `[!...]` negation; leading `]` = empty set; an UNTERMINATED `[` matches NOTHING) and is cross-checked against an independent Ruby oracle. Third-party actions full-SHA pinned. |
| `scripts/smoke-action-bundle.mjs` | new | Deterministic release smoke harness: runs a committed `index.js` bundle in an isolated self-test mode (`CHAOS_STUDIO_SMOKE_CHECK=1`, scrubbed env), requires EXACTLY exit 0, kills the whole process tree on timeout (POSIX process group / Windows Job Object via `scripts/win-job.ps1`) with a bounded post-kill deadline, and propagates crashes/timeouts. Invoked by the Action release `validate` gate and the OneBranch task gate (no `|| true`). |
| `scripts/win-job.ps1` | new | Windows Job Object launcher/killer for the smoke harness: creates a `KILL_ON_JOB_CLOSE` job and launches the bundle with `PROC_THREAD_ATTRIBUTE_JOB_LIST` so the child is a job member ATOMICALLY at creation (no suspend/resume window in which it could orphan), so the ENTIRE tree is terminated atomically and VERIFIABLY on timeout — robust even after the root exits while a re-parented grandchild lingers (which `taskkill /t` cannot reach). |
| `packages/core/src/release/tagRulesetEval.ts` | new | The SINGLE operation-aware, LAYERED tag-ruleset evaluator (pure, typed, unit-tested) shared by the standalone release-protection verifier and mirrored by the workflow preflights: faithful GitHub FNM_PATHNAME coverage over `/`-segments, per-operation restriction (creation+update+deletion+`non_fast_forward`) required across ALL applicable active tag rulesets, ANDed layering (no applicable layer may grant a foreign bypass; every restricting layer must grant the release identity an `always` User bypass), and fail-closed on missing `bypass_actors` or unsupported fnmatch syntax. Also exports `evaluateTagNamespace`/`patternCoversNamespace` for NAMESPACE-WIDE coverage: a finite list of exact tags cannot pass — an active tag ruleset must have a WILDCARD include covering the whole `refs/tags/<prefix>*` namespace (no excludes) that restricts every op and grants the sole identity bypass, so a ruleset matching only the finite probe refs is rejected. |
| `scripts/lib/eval-tag-ruleset.mjs` | new | Thin CLI wrapper (`node`) around `packages/core/src/release/tagRulesetEval.ts` used by `scripts/verify-release-protections.sh`: reads the active tag rulesets, release-identity id, and required-ops from env and, in PER-REF mode (`TAG_REF`) or NAMESPACE mode (`NAMESPACE_PREFIX`, `evaluateTagNamespace`), exits non-zero with `::error::` lines on any misconfiguration — so the standalone verifier and the release-time preflights apply the same rules. |
| `packages/core/src/release/workflowPermsAudit.ts` | new | Shared, pure, unit-tested static auditor of GitHub Actions workflow files for release-authority write vectors the DEFAULT-token audit cannot see: an indentation-aware scanner of the `permissions:` grammar (inline `read-all`/`write-all`/`{}`/`{scope: level}` and block maps, at workflow AND job level) that also handles QUOTED KEYS (`"permissions":`), YAML ANCHORS/ALIASES (`permissions: &p write-all` / `permissions: *p`, resolved; an UNRESOLVABLE alias fails closed), and QUOTED `uses:` values. It flags any content-bearing (`contents`/`packages`) `write`, a BROAD `write-all` (or unresolvable alias), and any REUSABLE-workflow invocation (`uses:` naming a `.yml`/`.yaml`, distinguished from a `repo@sha` action reference). Allowlisting downgrades a NARROW content-write to a NOTE but NEVER blesses a `write-all`; a reusable-workflow call is always a finding. A shared `flowContextKeys` helper decodes every flow-context `key:` (double-quoted/escaped/single-quoted, incl. the long-form `\UXXXXXXXX` escape) so an escaped `permissions` key inside a flow mapping is audited, and `scanFlowFormUses` flags a reusable-workflow `uses:` inside a flow map (resolving `*alias`; unresolvable fails closed). It also strips YAML NODE TAGS (`!!str`/`!Custom`/`!<…>`) from values, resolves MERGE KEYS (`<<: *anchor` splicing a `permissions:`/`uses:` grant — treated as ungated, fail closed) via `scanMergeKeys`/`scanMergedMapping`, handles ESCAPED-QUOTE double-quoted keys, splits COMPLEX flow values on top-level commas with a QUOTE-AWARE splitter/terminator (`splitTopLevelFlowEntries`/`flowScalarTerminator`) so a quoted flow value containing a comma is read whole, NORMALIZES YAML EXPLICIT-KEY blocks (`? key`/`: value`, including a TAGGED key `? !!str permissions` and an ANCHORED key `? &pk permissions` and a MULTILINE `?`-alone key) to `key: value` via `normalizeExplicitKeys`, gathers a MULTILINE quoted scalar value (`permissions: "write-\`…`  all"` and a multiline `uses:` value) via `readMultilineQuotedScalar`, decodes an ESCAPED block-form `uses` KEY (`"use\u0073":`) via `matchKeyLine`, classifies an INDENTED PLAIN SCALAR permissions value / scope LEVEL (`permissions:\n  write-all` / `contents:\n    write`), and a FLOW EXPLICIT key (`{ ? permissions : write-all }` — `flowContextKeys` accepts a `?`-preceded key, `parseInlinePermissions` strips the `?`/anchor marker). `flowContextKeys` also skips TAG (`!!str`/`!Custom`/`!<…>`) and ANCHOR (`&name`) node-property tokens preceding a FLOW key, so a TAGGED/ANCHORED flow explicit key (`{ ? !!str permissions : write-all }`, `{ &pk permissions: write-all }`) is recognized rather than missed (pass 51 #2). It additionally FAILS CLOSED (`detectUnsupportedYamlNodes`) on advanced YAML the indentation scanner cannot model — an alias used as a mapping KEY (`*a:`/`? *a`), a merge key splicing a SEQUENCE of aliases (`<<: [*a, *b]`/block-seq), and a non-scalar/custom TAG or flow-SEQUENCE value on a `permissions`/`uses` key — rather than silently pass a hidden grant (a single-alias merge and a scalar `!!str` tag remain handled) (pass 52 #2). A shared `stripNodeProperties` now strips a YAML tag AND anchor in ANY ORDER (so a `!!str &a write-all` tag-before-anchor grant is classified), and `readFlowScalarMultiline` gathers a MULTILINE flow permissions scalar value (`{ permissions:\n  write-all }`) so it is not read as empty (pass 53 #2). |
| `scripts/lib/audit-workflow-perms.mjs` | new | Thin CLI wrapper (`node`) around `packages/core/src/release/workflowPermsAudit.ts` used by `scripts/verify-release-protections.sh`: reads a JSON array of `{name, content}` workflow files (`WORKFLOWS_FILE`) plus an `ALLOWLIST` CSV of write-permitted workflow names, prints NOTE/`::error::` lines, and exits non-zero on any hard finding. |
| `scripts/verify-release-protections.sh` | new | One-shot operator verifier (`gh`+`jq`+`node`, read-only) that confirms every EXTERNAL release control the workflows depend on: the `release`/`mcp-release`/`pypi` environments are hardened (required reviewers + `prevent_self_review=true` + `can_admins_bypass=false` + single default-branch policy), `RELEASE_IDENTITY_ACTOR_ID` is set, the `v*`/`mcp-v*` tag rulesets lock every write+force operation to the single release identity via the SHARED operation-aware LAYERED evaluator (`scripts/lib/eval-tag-ruleset.mjs` — creation+update+deletion+`non_fast_forward`, ANDed across all applicable active tag rulesets, no foreign bypass, and the release identity bypasses every restricting layer) — evaluated against MULTIPLE representative refs per namespace (floating major `v1`, exact `v1.2.3`, a different major, and a multi-digit version; and the mcp-v equivalents) AND a NAMESPACE-WIDE coverage check (`evaluateTagNamespace`) that fails unless a wildcard include covers the entire `refs/tags/<prefix>*` namespace — so a ruleset whose include is a finite enumeration of exact tags cannot pass the finite probes and leave future tags unprotected, the `release`/`mcp-release` environments hold the `ACTION_RELEASE_TOKEN` secret, and repository **immutable releases** are enabled; it also runs a fail-closed release-AUTHORITY audit that REQUIRES a non-empty TYPED writer set (`user:`/`team:`/`app:` CSV, arg 3) and enumerates EVERY write surface — direct collaborators, teams, ORGANIZATION OWNERS (admins of every repo), the organization base repository permission, ACTIVE GitHub App installations with `contents:write` that reach this repo (suspended installations are inactive and excluded; `all`/`selected` repository selection is modelled, selected fails closed), read-write deploy keys, the EFFECTIVE Actions workflow authority (repo AND org default workflow token read-only AND PR-review approval disabled at BOTH levels), AND — via the shared `scripts/lib/audit-workflow-perms.mjs` auditor over EVERY workflow file on the default branch (enumerated as `jq -c` JSON objects so a newline in a path cannot split into phantom entries, and fetched BY BLOB SHA so a `#`/newline in a filename cannot truncate the request or evade the SHA-pinned audit) — EXPLICIT workflow/job `permissions:` grants of content-write/`write-all` (beyond the read-only default; the gated release workflows are allowlisted) and any REUSABLE-workflow `uses:` call — FAILING acceptance unless the sanctioned set EQUALS the actual writer set EXACTLY in BOTH directions (a sanctioned entry that is not an actual writer fails too), if a read-write deploy key exists, if the token is write/PR-approving, if any workflow grants un-gated write or calls a reusable workflow, or if any surface (including app installations + org owners, which require an org repo to enumerate) cannot be fully enumerated. A branch-policy or ruleset API/jq failure is RECORDED and the verifier CONTINUES (every API/jq op is guarded; the script does not use `set -e` and never aborts before the final aggregate), and failure tracking is an incrementing ERROR COUNT (not a 0/1 boolean) so a second failing control is never falsely reported OK. Same fail-closed logic the publish preflights enforce, runnable ahead of a release; referenced from the CONTRIBUTING.md checklist. |
| `scripts/verify-ado-signing-protections.sh` | new | One-shot operator verifier (`curl` + `jq`, read-only) for the EXTERNAL Azure DevOps controls that bind the OneBranch SIGNING and PUBLISH to the trusted branch. It authenticates to the Azure DevOps REST API with `AZURE_DEVOPS_EXT_PAT` as HTTP Basic auth (empty username) — the PAT is the credential actually consumed, with no Entra fallback, so an unscoped/missing PAT fails closed. Because `onebranch.pipeline.signing@1` takes NO service connection, signing is gated by a pipeline ENVIRONMENT (`ChaosStudio-ESRP-Signing-Prod`) and publishing by the `ADO-Plugin Publishing` service-connection ENDPOINT; the verifier resolves EACH (environment via the DISTRIBUTED TASK route `_apis/distributedtask/environments` vs service-connection endpoint) and FAILS CLOSED unless it carries (a) an ENABLED Branch Control check — `isDisabled` must be the EXACT boolean `false` (a missing property, a null, or a `"false"` string all fail closed) — identified by its IMMUTABLE TASK GUID (`definitionRef.id`, NOT a spoofable display name or mutable task name) with the REQUIRED exact pinned task VERSION (`definitionRef.version` must equal the mandatory `BRANCH_CONTROL_TASK_VERSION` — the FULL version string, e.g. `0.0.1`, NOT a major-only `0`; the verifier exits if it is unset), `allowedBranches` == exactly the trusted branch and no unknown-status-branch allowance; (b) pipeline permissions authorizing EXACTLY the one official pipeline (a required numeric id argument) and no other (and not all pipelines); and — for the publish endpoint — (c) EXACTLY ONE ENABLED Approval check (again `isDisabled == false`) with at least one approver and `requesterCannotBeApprover=true` (the signing environment is gated by Branch Control + pipeline permissions and does NOT require an approval — reconciled with CONTRIBUTING.md). It ALSO reads the OFFICIAL build DEFINITION as a server-side CONSISTENCY/tamper check (default branch == trusted branch; YAML path == the reviewed `.pipelines/OneBranch.Official.yml`, `EXPECTED_YAML_PATH`) — which catches a swapped definition/retargeted YAML but does NOT by itself prevent a feature-branch run; the authoritative, non-REST-queryable prevention is the ESRP Real-SignType entitlement plus the CodeSign Validation gates (documented honestly in CONTRIBUTING.md). Mirrors the GitHub verifier's guarded, incrementing-error-count, aggregate-exit style; referenced from the CONTRIBUTING.md checklist and the OneBranch signing-task comment as the live half of the external signing binding. |
| `.github/workflows/release-action.yml` | new | Dispatch-only Action release in THREE isolated jobs — `validate` (runs release-commit code, no secret; empty-dir full-set + normalized-mode + blob-hash dist drift gate that rejects non-regular rebuilt entries; deterministic smoke harness), `package` (no repo code, no secret; authenticates the committed dist blobs directly from the immutable commit via bare-Git `ls-tree`/`archive` into a NUL-safe manifest + deterministic gzip archive), and `publish` (secret-bearing, no repo code; fail-closed release-environment PREFLIGHT via the API, bare-Git tag writes, re-derives the manifest from the tag commit, stages a DRAFT release + verifies the exact asset set/digests then publishes atomically and — after publication — requires GitHub release IMMUTABILITY (`isImmutable=true`) and re-verifies the asset set/digests, gated by a dedicated repository-immutability preflight step that runs BEFORE any tag write AND a JUST-IN-TIME immutability re-check immediately before the floating-major tag push (fail closed), a fail-closed release-token identity check (no early-exit skip), a repository-admin gate on BOTH github.actor and github.triggering_actor that is RE-RUN in the publish job's first step (so re-running only the failed publish job cannot bypass the validate-job authorization), and a Ruby-`File.fnmatch`-faithful FNM_PATHNAME matcher that evaluates only GitHub's SUPPORTED ruleset subset and FAILS CLOSED on unsupported syntax (backslash escapes, `[^...]` negation), with class-aware segmentation (a `/` inside `[...]` is not a separator; a reversed range matches EXACTLY its two endpoints; leading `]` = empty set; an UNTERMINATED `[` matches NOTHING) cross-checked against an independent Ruby oracle; the release OBJECT is RESERVED as a DRAFT by the release identity BEFORE the exact tag is made public (a best-effort race-NARROWER — one release per tag narrows, but does not eliminate, the window in which another `contents:write` holder could publish/alter/delete it; a draft is NOT exclusive ownership, so the publish-time asset-set + digest + GitHub-immutability verification is the actual gate on what ships, and restricting `contents:write` to the release identity is the external control that would *prevent* rather than *detect* tampering), and advances the floating major, repairing incomplete drafts). Both entry jobs gate on `github.ref_type == 'branch'` and the full `refs/heads/<default>` ref; no `${{ }}` in any shell body; SemVer no-leading-zero guard; per-major publish serialization + lease-then-revalidate floating-major advance; existing-release verification via Git refs. |
| `.pipelines/OneBranch.Official.yml` | new | OneBranch Official for the Azure extension in THREE isolated stages: `build` (builds/packages an UNSIGNED VSIX with NO Marketplace credential and NO signing; deterministic smoke harness, no `|| true`; AND a Node20_1-runtime-parity gate that switches to Node 20 and `node --check`-parses every staged task bundle plus runs the deterministic smoke harness under Node 20 — matching the task's `Node20_1` execution handler rather than only the Node 24 build), `sign` (a DEPLOYMENT job that TARGETS the protected pipeline ENVIRONMENT `$(SigningEnvironment)` = `ChaosStudio-ESRP-Signing-Prod`, so ADO evaluates that environment's Approvals-and-checks — Branch Control limited to `refs/heads/main` + pipeline permissions restricted to the official pipeline (the human Approval gate is on the PUBLISH resource, NOT signing — reconciled with CONTRIBUTING.md and the verifier) — as an UNAVOIDABLE gate BEFORE any signing step runs; the environment binding, not an editable in-YAML condition, is what gates `external_distribution` signing; runs the governed `onebranch.pipeline.signing@1` task (supported inputs `signing_environment: azure_devops` + `signing_profile: external_distribution`; NO service connection) + governed CodeSignValidation@0 + PostAnalysis@2 (`CodesignValidationBreakOn: WarningAbove`, `ToolLogsNotFoundAction: Error`) on the build stage's artifact; the INDEPENDENT enforcement of `external_distribution` signing is OneBranch PLATFORM governance (a Real SignType is granted only to the onboarded Official pipeline), external to the YAML — the `environment:` binding is defense in depth; verified live by `scripts/verify-ado-signing-protections.sh` and documented in CONTRIBUTING.md, so a feature-branch run builds an UNSIGNED VSIX and can never reach trusted signing) and `publish` (a OneBranch v2 `releaseJob` with a declared signed-VSIX artifact input from the `sign` stage and `checkout: none`; runs ONLY on the trusted default branch with `publishExtension` set; executes NO repository code; INDEPENDENTLY RE-VERIFIES the signed-artifact handoff by running CodeSignValidation@0 + PostAnalysis@2 on the consumed VSIX before spending the Marketplace credential — a run that skipped or faked signing cannot publish; the authoritative credential gate is branch control + approvals on the `ADO-Plugin Publishing` service connection, documented in CONTRIBUTING.md). |
| `.pipelines/OneBranch.PullRequest.yml` | new | OneBranch PR validation (packages the dev VSIX; does not publish); path filter includes the build-critical inputs and `action.yml` (the tests consume it). |
| `.config/CredScanSuppressions.json` | new | CredScan suppressions referenced by the OneBranch SDL. |
| `.github/dependabot.yml` | mod | Adds the `npm` (`/`) update ecosystem next to the existing `pip` and `github-actions` ecosystems (only `npm` is added; the others already existed). |
| `.github/CODEOWNERS` | mod | Adds CI/CD-integration ownership for the new paths (and this inventory). |
| `README.md` | mod | Documents the Action/task surface and SHA-pins the referenced `azure/login`. |
| `CONTRIBUTING.md` | mod | Adds the TypeScript workspace build/test guidance, the committed-`dist/` rebuild rule, the required `release` GitHub Environment configuration (required reviewers, exact default-branch deployment policy, `ACTION_RELEASE_TOKEN`), and the required `ADO-Plugin Publishing` service-connection security for the OneBranch extension publish (branch control limited to the default branch, required approvals, and pipeline-permission restriction). |
| `copilot-cli-plugin/CONTRIBUTING.md` | mod | Documents the **dispatch-only** MCP release: maintainers run the `release` workflow (`workflow_dispatch`) from the default branch with a `version` input; the workflow itself creates the protected `mcp-v*` tag (no maintainer-pushed trigger tag). |
| `E1-INVENTORY.md` | new | This authoritative task→file inventory. |

## Cross-task coverage (no additional files)

Some files contribute to more than one task. To keep the exactly-once guarantee,
each is inventoried under a single owning task above; its secondary role is recorded
here as a relationship, not as a duplicate row:

- **`.gitattributes`** — owned by **E1-T1** (pins the `dist/**` bundles to LF). Also
  serves **E1-T3** by pinning `packages/core/fixtures/source-extracts/**` to LF so the
  reviewed-extract provenance hashes are reproducible cross-platform.
- **`packages/core/src/contract.ts`** — owned by **E1-T2** (the canonical types +
  adapter interfaces). Also carries the **E1-T4** DX1–DX3 encodings (see the E1-T4
  section, which adds no new files).
- **`packages/core/fixtures/**`** — owned by **E1-T3** (fixtures, reviewed extracts,
  provenance). Also carries the **E1-T4** DX1–DX3 wire corrections.

## Ignored planning file (not committed)

| File | State | Note |
|---|---|---|
| `.octane-input/cicd-integrations.plan.md` | mod (ignored) | The E1 status and task rows are updated here. This path is excluded from version control via `.git/info/exclude`, so it is intentionally **not** part of the committed change set; it is listed here for completeness so the "ignored planning-file update" is accounted for in the authoritative inventory. |

## Change-set reconciliation & corrective-pass baseline

> **Historical E1 review bookkeeping.** This section documents the state of the E1
> branch **at the time E1 was completed and committed**, not the current worktree
> HEAD (which has since moved on through E2–E6 corrective passes). All file states
> ("new"/"untracked" vs. "pre-existing"/"tracked") and the reproduction commands
> below are relative to the **E1 commit range** — they are not expected to reproduce
> against the present HEAD, where these paths are all tracked/committed.

This inventory is the **committed** record of E1's completion status (the acceptance
state that reviewers need lives here, not in the git-ignored plan file). At E1 time,
the worktree was a **cumulative** E1 branch: the full change set was **99 paths** —
**8 pre-existing files modified** (`.github/CODEOWNERS`, `.github/dependabot.yml`,
`.github/workflows/release.yml`, `.github/workflows/test.yml`, `.gitignore`,
`CONTRIBUTING.md`, `README.md`, `copilot-cli-plugin/CONTRIBUTING.md`) plus **91 new
untracked paths** — and every one of those 99 paths is inventoried in **exactly one**
E1-T1…E1-T5 task table above (the git-ignored plan file is the only additional entry,
called out explicitly as non-committed). Reviewers can reproduce this baseline against
the **E1 commit range** (e.g. `git --no-pager diff --name-only <pre-E1-base>..<E1-head>`
for the 8 tracked files, and `git ls-files --others --exclude-standard` run **at E1
head**, before those paths were committed, for the 91 untracked); the union equals the
inventoried set with no untraced path. Running these commands against the current HEAD
will not reproduce this snapshot, since all 99 paths are now committed history.

**Cumulative E1 base vs. this corrective pass.** The 99-path set is the *whole* E1
deliverable accumulated across the corrective passes, NOT the delta of any single pass.
The most recent **pass-61** corrective pass touched only the analyzer and its tests plus
this committed inventory:

| File | State | Pass-61 change |
|---|---|---|
| `packages/core/test/contract/source-extracts.ts` | new (cumulative E1-T3) | Generalized the escaped-`new`-identifier provenance from the `@`-only pass-60 signal to ALL non-literal spellings: `normalizeMaskedIdentifiers` now marks any token that normalizes to `new` from a RAW source slice other than the literal `new` (a verbatim `@new`, a Unicode-escaped `n\u0065w`, or a removed-Cf-format spelling) with a synthetic `@`, because a keyword can carry no `@`/escape/format char. `classifyNewTypeAt`'s `masked[q] === '@'` guard now correctly ends tracking for those non-`@` escaped local functions (which pass-60 misread as object initializers), while the literal `new` keyword's object-creation positives are retained. Corrected the stale comment claiming `@` is the mandatory identifier spelling. |
| `packages/core/test/contract/source-extracts.test.ts` | new (cumulative E1-T3) | Pass-61 Unicode-escaped (`n\u0065w`, nullable/void return) and Cf-format-char `new` local-function negatives with a retained literal-`new()` object-initializer positive guard. |
| `E1-INVENTORY.md` | new (cumulative E1-T5) | This reconciliation + the pass-61 test-row note. |

Immediately prior, **pass-60** preserved `@new` provenance through normalization (root-fixing
the nullable/tuple return-type cases), **pass-59** distinguished the `new` keyword from an escaped `@new`
local function by context, **pass-58** recognized anonymous/target-typed/alias-qualified object
initializers, **pass-57** introduced the precise `braceContext` classifier, **pass-56**
added `isInitializerMemberAssignment`, **pass-55** bounded the tracker to the instance's
lexical lifetime, and **pass-54** added R1/R2/R3 — each with regression tests — in the same
three files.

Because the E1 base files are **untracked**, a per-pass textual diff is not derivable from
git alone; the per-pass provenance is therefore recorded here (and in the committed test
names, each tagged `pass NN finding …`). All other 96 inventoried paths are unchanged by
the corrective passes and belong to the established E1 base.

